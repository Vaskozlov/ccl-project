if (MSVC)
    include(cmake/.cmake-msvc)
elseif(CMAKE_CXX_COMPILER_ID MATCHES "Clang")
    include(cmake/.cmake-unix)
    include(cmake/.cmake-clang)
elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
    include(cmake/.cmake-unix)
    include(cmake/.cmake-gcc)
endif()
